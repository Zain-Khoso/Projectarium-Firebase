rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /projects {
      match /thumbnails/{allPaths=**} {
        allow read;
        allow create: if request.auth != null;
      }
    }
  }
}
